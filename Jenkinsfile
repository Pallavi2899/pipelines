pipeline {
    agent any

    stages {
        stage('git scm checkout') {
            steps {
                git 'https://github.com/Pallavi2899/pipelines.git'
            }
        }
        
    
}}
